# ATOMES Project

## Objective and Protocol

Objective : Reconstruction of the structure factor of urea

## Simple Launch

### Prerequisites : virtual environmment

- Install `uv` : 

For MacOS and Linux users : 

```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```

Powershell users : 

```bash
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
```


- Clone the repository

### Installation

From the project root, run:

```bash
uv sync
```

This command creates the project environment and installs all dependencies specified in `uv.lock`.

### Running the Project

To launch the project, run:

```bash
uv run python main.py
```

This command executes `main.py` within the project's managed environment.


## Using Multiwfn

This project uses the Multiwfn software (http://sobereva.com/multiwfn/) to determine the electronic densities of the systems under consideration. The git repository does not directly call Multiwfn; instead, the text files generated by the software were manually imported.

## Code Structure

Throughout the project, data are organized in a dictionary whose keys correspond to the atoms of the molecule under study (urea in this case). Various properties are progressively associated with each atom, including atomic numbering, electronic densities, Hirshfeld weights, and other related quantities.

### Multiwfn_data

Folder containing the .txt files generated by Multiwfn.

### Extract_Parameters

Scripts used to read the different files generated by Multiwfn.

### Grid

A 3D grid (spatial discretization) is created, on which all quantities of interest throughout the project are calculated.

Scripts:

basis_functions.py :$\$
becke_grid.py : $\$
integral_calcul.py :$\$
urea_grid_multiwfn.txt : Text file containing the values of $x, y, z$ for the grid (exported from becke_grid.py)

### Partitioning

The scripts related to this part are located in the /Partitioning folder.

The Hirshfeld weight of an atom in a molecule, as a function of position, is given by:

$\omega_j(r) = \frac{\rho_j(r)}{\rho^M(r)}$

With:

* $\rho_j(r)$ the spherical electronic density of an isolated atom
* $\rho^M(r)$ the electronic density of the promolecule

Scripts:$\\$
calcul_weights.py: computes Hirshfeld weights using the formula above
export_weights.py: exports the Hirshfeld weights to a text file
plot_weights.py: plots the Hirshfeld weights for a given atom

### Calculation of Scattering Factors

In the Residual_densities folder

The code is divided into several parts:

Two files that retrieve the structure factors from the files 'FS expérimentaux.hkl' and 'FS Théorique + Phase.out':
'Importation facteur de structure expérimentaux.py': retrieves the data from 'FS expérimentaux.hkl' and creates a dictionary stored in FS_expérimentaux_dict.py containing a list with the squared modulus of the experimental structure factors and the standard deviation of this measurement.

'Importation facteur de structure théorique et phases.py': retrieves the data from 'FS Théorique + Phase.out' and creates two dictionaries stored in FS_théorique_dynamique_dict.py and FS_théorique_statique_dict.py, each containing a list with the real part, the imaginary part, and the theoretical phase of the structure factors.

For 3D:
Then we compute the Fourier transform on a 3D grid:

'Calcul 3D Grid.py': This program computes the residual densities and stores them in the 3D grid 'delta_rho_flat.npz'

Then we display the interactive 3D map:

'Plot 3D.py': We can choose different saturation levels; we chose 10 e⁻/Å³ to prevent very high density values from distorting the display.

For 2D:

'Calcul 2D + Plot.py': Running this file, we are asked to choose one of the two molecule types, A or B, for which to display the residual density, then the (h, k, l) triplet of the molecule to display. The file returns the residual density, the position of the molecule within the unit cell in 3D, and the unit cell itself, to verify that we are plotting the correct crystal cell (initially we wanted to highlight the selected atom within the cell, but we haven't managed to do that yet).



