[project]
name = "atomes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.4.4",
    "pandas>=3.0.3",
    "plotly>=6.6.0",
    "taskipy>=1.14.1",
]



[dependency-groups]
dev = [
    "ruff>=0.15.4",
    "ty>=0.0.20",
    "pre-commit>=4.5.1",
	"ipykernel>=7.2.0",
    "pytest>=9.0.2"
]

[tool.taskipy.tasks]
lint = "uv run ruff check . --fix"
ty = "uv run ty check ."
